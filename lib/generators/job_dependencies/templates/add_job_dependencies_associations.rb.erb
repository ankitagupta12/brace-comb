# This migration creates the `dependencies` table
class AddJobDependenciesAssociations < ActiveRecord::Migration<%= migration_version %>
  def change
    add_foreign_key :job_dependencies, :jobs, column: :source_job_id, dependent: :delete
    add_foreign_key :job_dependencies, :jobs, column: :destination_job_id, dependent: :delete
  end
end

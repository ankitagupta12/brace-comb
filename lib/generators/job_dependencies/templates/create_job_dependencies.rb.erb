# This migration creates the `dependencies` table
class CreateJobDependencies < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :job_dependencies do |t|
      t.integer :dependency_type
      t.integer :source_job_id
      t.integer :destination_job_id
      t.timestamps null: false
    end
    add_index :job_dependencies, %i(source_job_id destination_job_id)
  end
end

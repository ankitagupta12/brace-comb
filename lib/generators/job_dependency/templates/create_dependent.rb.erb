# This migration creates the dependent table
class <%= dependent_table_name.camelize %> < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :<%= dependent_table_name %> do |t|
      t.timestamps null: false
    end
  end
end
